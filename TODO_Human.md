# TODO — 사람의 확인 및 작업 필요

이 파일에는 사람의 계정·권한·결제 승인, 법률 또는 제품 판단, 주관적인 피드백,
프로덕션 콘솔 접근, 실제 기기 검증이 필요한 작업을 기록합니다. AI 에이전트는 새로운
사람 의존 작업이 생길 때마다 이 파일을 갱신해야 합니다.

## Apple 및 iOS 프로덕션

- [ ] 조직 또는 소유자 명의로 Apple Developer Program에 가입하고 최신 Apple 계약에
      동의합니다. App Store 배포, 프로덕션 Apple 로그인, StoreKit 상품, TestFlight,
      안정적인 iOS Development Build를 진행하려면 필요합니다.
- [ ] `com.mulist.app`의 프로덕션 App ID, 인증서, 프로비저닝 프로파일, Apple 로그인
      권한 및 App Store Connect 앱을 생성합니다.
- [ ] 구독 구현에 StoreKit 2와 RevenueCat 중 무엇을 사용할지 결정하고, 상품 ID를
      생성한 뒤 구독 복원 및 결제 관리 흐름을 구성합니다. Premium 기능을 활성화하기
      전에 Sandbox 환경에서 구매를 테스트합니다.
- [ ] 목표 기기인 11인치 iPad에서 Apple Pencil 포인터 인식, 필기, 핀치 줌, 네 가지
      이동 방식, 두 쪽 보기 렌더링, 세로 이동 및 페이지 터치 스냅을 실제 짧은 악보와
      긴 악보로 테스트합니다.

## Firebase 프로덕션 및 보안

- [ ] 프로덕션 Firestore와 Storage의 리전 및 Firebase Blaze 요금제를 확정합니다.
      Callable/Scheduled Functions를 배포하기 전에 예산 한도와 이메일 알림을 설정합니다.
- [ ] 변경된 Firestore Rules, Storage Rules, Functions 및 Web Client를 배포합니다.
      배포 전에는 실시간 카탈로그 조회와 관리자용 Callable Functions를 프로덕션에서
      사용할 수 없습니다.
- [ ] iOS와 Web에 Firebase App Check를 구성하고 적용합니다. 이후 임시로
      `enforceAppCheck: false`를 사용하는 Callable Functions를 프로덕션 강제 적용으로
      변경합니다.
- [ ] 필요한 플랫폼 인증 정보를 준비한 후 Google 로그인 OAuth와 Apple 로그인을
      구성하고 테스트합니다.
- [ ] 프로덕션 Firebase Console에서 이메일/비밀번호 인증, 승인된 도메인, 비밀번호
      정책 및 계정 복구 설정을 확인합니다.
- [ ] 관리자 페이지에 추정치가 아닌 실제 청구 금액을 표시하려면 Cloud Billing Export
      또는 다른 신뢰할 수 있는 결제 데이터 소스를 연결합니다.

## 웹 관리자 기능 검증

- [ ] 배포된 Web Client에 `sion@sionuu.com`으로 로그인하여 관리자 메뉴가 표시되는지
      확인합니다. 다른 계정이 엔드포인트를 직접 호출하더라도 관리자 Function을 사용할
      수 없는지 확인합니다.
- [ ] 관리자 페이지에서 최초의 전체 태그·아티스트 카탈로그를 저장합니다. iPad에서
      실시간으로 반영되는지, 오프라인 상태에서는 캐시된 카탈로그를 사용하는지 확인합니다.
- [ ] 프로덕션 환경에서 사용자 목록 조회, 사용자 활성화/비활성화, 정규화 카탈로그
      검증, Storage/Firestore 지표 및 관리자 감사 로그가 정상적으로 작동하는지 확인합니다.
- [ ] 선택한 Firebase 리전의 최신 공식 가격과 비용 계산기의 Storage, 네트워크 송신,
      Firestore 읽기 및 쓰기 단가를 비교합니다. 자동 공식 가격 조회가 차단되었으므로
      현재 기본값을 공인된 가격으로 간주하면 안 됩니다.
- [ ] 관리자 페이지의 카탈로그 JSON 편집 사용성, 누락된 운영 도구, 사용자 관리의
      안전성, 지표의 유용성 및 비용 계산기의 가정에 관해 사람의 제품 피드백을 받습니다.
      채택한 피드백은 `TASKS.md`의 작업으로 등록합니다.
- [ ] 현재 Firebase Auth 사용자 목록은 최초 1,000명까지만 표시됩니다. 페이지네이션이
      필요한지, 사용자 영구 삭제 및 내보내기 기능이 필요한지 결정합니다.

## 다국어 및 카탈로그 품질

- [ ] 일본어와 영어 원어민 검토자에게 라이브러리 빈 화면, 프로필, 악보 설정,
      클라우드 동기화, 음악 파트 및 태그 이름이 자연스러운지 검토받습니다.
- [ ] 카탈로그를 대규모로 확장하기 전에 사람이 정규 태그 ID, 다국어 이름, 별칭,
      아티스트 ID, 활동명, 로마자 표기 및 동명이인 처리 정책을 검토합니다.
- [ ] `아이유-곡명`, `IU - Title`, 피처링 아티스트, 하이픈이 포함된 밴드 이름,
      버전 접미사 및 아티스트 별칭이 일반 단어와 겹치는 파일명 등 실제 명명 규칙으로
      파일명 감지 기능을 테스트합니다.

## OCR, 법률 및 운영

- [ ] 한국어, 영어 및 일본어를 지원하면서 Expo Development Build와 호환되는 온디바이스
      OCR 엔진을 선정하고 라이선스를 확인합니다. 처리 파이프라인은 있지만 Expo Go에는
      필요한 인식기가 포함되어 있지 않습니다.
- [ ] 서비스 이용약관과 개인정보 처리방침 URL을 게시하고 법률 검토를 받습니다.
      법률 링크를 활성화하거나 프로덕션 구독 데이터를 수집하기 전에 완료해야 합니다.
- [ ] 공개 고객지원 이메일 주소를 정하고 설정한 뒤, 실제 기기에서 버그 제보, 기능 제안
      및 문의 흐름을 테스트합니다.
- [ ] 휴지통, Song Package, 공유 데이터, 관리자 감사 로그, 사용자 탈퇴, 백업 및
      Firebase Storage 수명 주기 정리에 관한 프로덕션 보존 정책을 검토합니다.

## 전체 흐름 인수 테스트

- [ ] 동일한 계정이 로그인된 iPad를 켜 둔 상태에서 배포된 Web Client로 여러 PDF를
      업로드합니다. 모든 패키지 파일이 준비된 뒤에만 자동 다운로드되는지, 오프라인 복구가
      작동하는지, 당겨서 새로고침으로 누락된 이벤트를 복구하는지 확인합니다.
- [ ] 로컬 전용 곡과 클라우드 연동 곡을 각각 하나씩 영구 삭제합니다. 로컬 SQLite와 파일,
      Firestore, Storage 및 두 번째로 연결된 iPad에서 모두 제거되고 새로고침 후에도 다시
      나타나지 않는지 확인합니다.
- [ ] 목표 iPad에서 대표적인 4쪽, 20쪽 및 대용량 스캔 PDF로 성능 검증을 진행합니다.
      PDF 단계별 시간 로그, 메모리 동작, 인접 두 쪽 프리렌더링 품질 및 셋리스트 캐시
      승격 동작을 기록합니다.
